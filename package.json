{
  "name": "agents",
  "private": true,
  "workspaces": [
    "agents/*"
  ],
  "scripts": {
    "dev": "bun --filter '*' dev",
    "build": "bun --filter '*' build",
    "test": "bun --filter '*' test"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
